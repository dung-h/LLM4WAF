# Minimal CRS setup (adjust as needed)
SecAction \
 "id:900000,phase:1,nolog,pass,\
  t:none,\
  setvar:tx.paranoia_level=2,\
  setvar:tx.inbound_anomaly_score_threshold=10,\
  setvar:tx.outbound_anomaly_score_threshold=4"

